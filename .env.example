# Copiar para .env e preencher (wrangler carrega .env ao correr npm run deploy)
# D1: Dashboard → Workers & Pages → D1 → prompt-library-db → UUID
D1_DATABASE_ID=

# Mesmo valor que LIBRARY_WRITE_SECRET no Pages (Functions) — para wrangler pages dev local (.dev.vars)
# LIBRARY_WRITE_SECRET=

# Se o projeto Pages no dashboard tiver OUTRO nome, ajusta --project-name em package.json → script "deploy"
